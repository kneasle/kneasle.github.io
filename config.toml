base_url = "https://kneasle.github.io"
compile_sass = true
# Whether to build a search index to be used later on by a JavaScript library
build_search_index = true

taxonomies = [
    { name = "Topic", feed = false }, # Rust, Python, etc.
    { name = "Language", feed = false },  # Computing, Ringing, etc.
]

[markdown]
highlight_code = true

[extra]
# Put all your custom variables here
